services:
  lookbook:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - reader-data:/data
    environment:
      - AUTH_SECRET=${AUTH_SECRET:-change-me-to-a-random-string}
    restart: unless-stopped

volumes:
  reader-data:
